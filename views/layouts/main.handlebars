<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="justify-flex-start align-center p-3 pl-5 bg-dark">
      <nav class="display-flex justify-space-between">
        <h5>
        <a class="mx-3" href="/">Home</a>
        <a class="mx-3" href="/dashboard">Dashboard</a> 
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button class="mx-3 no-button" id="logout"><h5>Logout</h5></button>
        {{else}}
        <a class="mx-3" href="/login">Login</a>
        {{/if}}
        </h5>
      </nav>     
      
    
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
